# Core dependencies (always required)
ollama
chromadb
python-dotenv

# Testing
pytest

# Optional provider backends — install whichever you need:
#   pip install anthropic        # for AnthropicBackend
#   pip install huggingface_hub  # for HuggingFaceBackend
